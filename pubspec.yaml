name: scooter_mobile_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.11.5 <4.0.0' # Fixes the strict version constraint mismatch

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  mqtt_client: ^10.11.11
  geolocator: ^14.0.3
  file_picker: ^10.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
